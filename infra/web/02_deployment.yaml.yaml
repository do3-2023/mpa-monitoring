apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: mpa-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      name: web
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        name: web
    spec:
      containers:
        - image: ghcr.io/do3-2023/mpa-monitoring/web:v4.0
          imagePullPolicy: Always
          name: web
          env:
            - name: VITE_API_ENDPOINT
              value: "http://162.38.112.168:32000"
          ports:
            - containerPort: 8080
              protocol: TCP
              name: httpweb
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 3
            failureThreshold: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 3
            failureThreshold: 10